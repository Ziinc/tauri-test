[package]
name = "tauri-test"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri-test-macros = { path = "../tauri-test-macros" }
serde_json = "1"
tauri = { version = "2", default-features = false }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
