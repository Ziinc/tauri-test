# tauri-test-macros

Implementation crate for `tauri-test`.

Most users should depend on `tauri-test` instead of using this crate directly.

This crate provides the procedural macros that generate command trampolines, dispatcher implementations, and test setup glue for Tauri command testing.
